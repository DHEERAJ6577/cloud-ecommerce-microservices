apiVersion: v1
kind: Service
metadata:
  name: cart-postgres
  namespace: ecommerce
spec:
  selector:
    app: cart-postgres
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP